{
  products(filter: { sku: { eq: "{{ $sku }}" } }) {
    items {
      id
      name
      related_products {
        id
        name
        thumbnail {
          url
        }
        url_key
        url_suffix
        price_range {
          maximum_price {
            regular_price{
              currency
              value
            }
          }
        }
      }
    }
  }
}
