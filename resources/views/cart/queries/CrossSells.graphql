{
  products(filter: { sku: { in: replace } }) {
    items {
      id
      name
      crosssell_products {
        id
        name
        canonical_url
        thumbnail {
          url
        }
        url_key
        url_suffix
        price_range {
          maximum_price {
            regular_price{
              currency
              value
            }
          }
        }
      }
    }
  }
}
